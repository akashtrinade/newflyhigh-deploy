spring.application.name=flyhigh-backend
server.port=${SERVER_PORT:8081}

# ── Profiles ── (set SPRING_PROFILES_ACTIVE=prod for AWS)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# ── MongoDB Atlas ──
spring.data.mongodb.uri=${DATABASE_URL}
spring.data.mongodb.auto-index-creation=${MONGODB_AUTO_INDEX:true}

# ── Mail (Gmail SMTP) ──
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:dev@localhost}
spring.mail.password=${MAIL_PASSWORD:dev-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_SSL_TRUST:smtp.gmail.com}
spring.mail.properties.mail.debug=false

# ── JWT ──
jwt.secret=${JWT_SECRET:ZGV2LWp3dC1zZWNyZXQtZm9yLWxvY2FsLWRldmVsb3BtZW50LW9ubHktZG8tbm90LXVzZS1pbi1wcm9kdWN0aW9u}
jwt.access-token-expiration=${JWT_ACCESS_EXPIRY:900000}
jwt.refresh-token-expiration=${JWT_REFRESH_EXPIRY:604800000}

# ── OTP ──
otp.length=${OTP_LENGTH:6}
otp.expiry-minutes=${OTP_EXPIRY_MINUTES:10}
otp.max-requests-per-hour=${OTP_MAX_REQUESTS_PER_HOUR:3}

# ── Admin ──
# No default credentials — must be provided via ADMIN_EMAIL/ADMIN_PASSWORD
# (or the dev profile's application-dev.yml). Startup fails if unset.
app.admin.email=${ADMIN_EMAIL:}
app.admin.password=${ADMIN_PASSWORD:}

# ── BCrypt ──
bcrypt.strength=${BCRYPT_STRENGTH:12}

# ── Security ──
server.servlet.session.cookie.secure=${COOKIE_SECURE:true}
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=strict
# Controls Secure flag on JWT cookies — set COOKIE_SECURE=false in .env for local HTTP dev
app.cookie.secure=${COOKIE_SECURE:true}

# ── CORS (comma-separated origins) ──
# Dev defaults cover the Vite dev server on its usual ports and both hostname
# variants (localhost / 127.0.0.1). Vite auto-increments to 5174/5175 when 5173
# is busy, and vite.config.ts binds 127.0.0.1 — both caused CORS errors on
# teammates' machines. Production MUST set CORS_ORIGINS explicitly.
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174,http://localhost:5175,http://127.0.0.1:5175,http://localhost:3000,http://127.0.0.1:3000}

# ── Logging ──
logging.level.com.flyhigh=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:INFO}

# ── Google OAuth ──
google.client-id=${GOOGLE_CLIENT_ID:dev-client-id}
google.client-secret=${GOOGLE_CLIENT_SECRET:dev-client-secret}

# ── WebSocket / Signaling ──
app.signaling.websocket-endpoint=${WEBSOCKET_API_ENDPOINT:http://localhost:5000}

# ── Razorpay ──
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_DevPlaceholderKey00000}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:DevPlaceholderSecret00000000000000000}
razorpay.currency=${RAZORPAY_CURRENCY:INR}
razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:}
razorpay.commission.percent=${RAZORPAY_COMMISSION_PERCENT:20}

# ── RazorpayX (automatic expert payouts) ──
# Blank keys → gateway disabled → payouts fall back to the manual admin flow.
razorpay.x.key.id=${RAZORPAYX_KEY_ID:}
razorpay.x.key.secret=${RAZORPAYX_KEY_SECRET:}
razorpay.x.account.number=${RAZORPAYX_ACCOUNT_NUMBER:}
razorpay.x.webhook.secret=${RAZORPAYX_WEBHOOK_SECRET:}

# ── Platform Commission ──
platform.commission.percent=${PLATFORM_COMMISSION_PERCENT:20}

# ── Pricing ──
pricing.rate-change-days=${PRICING_RATE_CHANGE_DAYS:14}

# ── Session ──
session.free-trial-seconds=${SESSION_FREE_TRIAL_SECONDS:300}
session.extend-prompt-seconds=${SESSION_EXTEND_PROMPT_SECONDS:300}

# ── Call Request Cleanup ──
# PENDING call requests no expert responds to are cancelled after this many
# minutes (UI's own wait timeout is 60s; TTL gives it slack).
call-request.pending-ttl-minutes=${CALL_REQUEST_PENDING_TTL_MINUTES:2}
call-request.cleanup.cron=${CALL_REQUEST_CLEANUP_CRON:0 * * * * *}

# ── Presence ──
# Seconds after an expert's last heartbeat before they are considered offline.
# Must stay >= the UI heartbeat interval (60s).
app.presence.online-window-seconds=${PRESENCE_ONLINE_WINDOW_SECONDS:120}

# ── Socket.IO Payment Server ──
socketio.payment.port=${SOCKETIO_PORT:8085}

# ── Security Headers ──
app.security.hsts-max-age=${HSTS_MAX_AGE:31536000}
app.security.csp-policy=${CSP_POLICY:default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self' https://api.razorpay.com wss:; frame-src 'self' https://api.razorpay.com; font-src 'self'}

# ── Resilience4j (payment gateway hardening) ──
# Circuit breaker prevents cascading failures when Razorpay is degraded.
# Retry with exponential backoff handles transient network errors and rate limits
# without overwhelming the gateway.
resilience4j.circuitbreaker.configs.default.failure-rate-threshold=50
resilience4j.circuitbreaker.configs.default.wait-duration-in-open-state=30s
resilience4j.circuitbreaker.configs.default.sliding-window-size=10
resilience4j.circuitbreaker.configs.default.minimum-number-of-calls=5
resilience4j.retry.configs.default.max-attempts=3
resilience4j.retry.configs.default.wait-duration=1s
resilience4j.retry.configs.default.exponential-backoff-multiplier=2

# ── Actuator (health checks for AWS / K8s) ──
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.endpoint.health.probes.enabled=true
management.health.mongo.enabled=true
# Keep health checks lightweight for Lambda cold starts
management.health.defaults.enabled=false
management.endpoint.health.group.liveness.include=livenessState
management.endpoint.health.group.readiness.include=readinessState,mongo
# Health endpoints should be accessible without auth
management.endpoints.web.path-mapping.health=/health
management.endpoints.web.path-mapping.info=/info

# ── Settlement ──
settlement.period-minutes=${SETTLEMENT_PERIOD_MINUTES:10}
settlement.worker.cron=${SETTLEMENT_WORKER_CRON:0 */1 * * * *}

# ── Payout ──
payout.worker.cron=${PAYOUT_WORKER_CRON:0 */10 * * * *}
payout.minimum-amount=${PAYOUT_MINIMUM_AMOUNT:1000}
# razorpayx = automatic RazorpayX transfers; manual = admin completes offline transfers.
# Effective value can be overridden by the persisted platform settings (admin UI).
payout.provider=${PAYOUT_PROVIDER:razorpayx}
payout.auto-create=${PAYOUT_AUTO_CREATE:false}
# PROCESSING gateway payouts older than this are re-checked against RazorpayX.
payout.reconcile-stuck-minutes=${PAYOUT_RECONCILE_STUCK_MINUTES:60}
# Per-transaction UPI payout limit (₹) — larger withdrawals require bank details.
payout.upi-max-amount=${PAYOUT_UPI_MAX_AMOUNT:100000}

# ── Refund ──
refund.auto-approve-threshold=${REFUND_AUTO_APPROVE_THRESHOLD:0}
